<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.goods.mapper.AdminRoleMapper">
    <resultMap id="BaseResultMap" type="com.gg.goods.entity.AdminRole">
        <result column="admin_id" jdbcType="VARCHAR" property="adminId"/>
        <result column="role_id" jdbcType="VARCHAR" property="roleId"/>
    </resultMap>

    <sql id="Base_Column_List">
        adminId, role_id
    </sql>

    <delete id="deleteByAdminIdAndRoleId" parameterType="com.gg.goods.entity.AdminRole">
        delete
        from admin
        where adminId = #{adminId,jdbcType=VARCHAR}
          and role_id = #{roleId,jdbcType=VARCHAR}
    </delete>

    <delete id="deleteByAdminId" parameterType="java.lang.String">
        delete
        from admin
        where adminId = #{adminId,jdbcType=VARCHAR}
    </delete>

    <insert id="insert" parameterType="com.gg.goods.entity.AdminRole">
        insert into admin (admin.adminId, role_id)
        values (#{adminId,jdbcType=VARCHAR}, #{roleId,jdbcType=VARCHAR})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        insert into admin_role (admin_id, role_id)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.adminId,jdbcType=VARCHAR}, #{item.roleId,jdbcType=VARCHAR})
        </foreach>
    </insert>

    <select id="selectByAdminId" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from admin_role
        where admin_id = #{adminId,jdbcType=VARCHAR}
    </select>

    <select id="selectByRoleId" parameterType="java.lang.String" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from admin_role
        where role_id = #{roleId,jdbcType=VARCHAR}
    </select>
</mapper>